# Example environment file
# Copy to .env and edit the values for your environment

APP_ENV=local
APP_DEBUG=true

# Database (local) - use SSH tunnel or local DB
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=questionnaire_app
DB_USER=root
DB_PASS=

# If using an SSH tunnel
DB_USE_SSH_TUNNEL=true
DB_TUNNEL_PORT=3307
