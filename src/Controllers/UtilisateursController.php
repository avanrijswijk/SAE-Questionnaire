<?php
// un utilisateur n'exhiste pas dans la base de données, il est représenter par un code obtenue à partie des informations répucpérées.

namespace App\Controllers;

class UtilisateursController {

    /**
     * Constructeur de la classe QuestionController.
     * Initialise les instances du modèle Question et du contrôleur Choix_possibleController.
     */
    public function __construct() {
    }

 function getCodeUtilisateur() {
        $code = '';
        //group
        if (true) {
            ;
        } else {
           ;
        }
        //identifiant
        if (true) {
            ;
        } else {
           ;
        }
        return $code;
    }
}